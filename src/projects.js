const contVerisage = ['development', 'depolyment', 'maintenance', 'design', 'wire frames', 'user stories']

const builtWith = [ 'React Native', 'Expo', 'Redux', 'Redux-Offline', 'Jest']

 const digNdirt = {
  title: 'DigNDirt',
  contributions: contVerisage,
  builtWith: builtWith, 
  src: require('./images/DNDImage.png')
}

 const haulNdirt = {
  title: 'HaulNDirt',
  contributions: contVerisage,
  builtWith: ['React Native', 'Expo'],
  src: require('./images/JobActions.png')
}

 const equipTracker = {
  title: 'EQ Tracker',
  contributions: contVerisage,
  builtWith: builtWith,
  src: require('./images/DNDImage.png')
}

export const projectArray = [digNdirt, haulNdirt, equipTracker]